name: Test Default Branch

on:
    workflow_dispatch:

jobs:
  build:
    name: Build  
    runs-on: ubuntu-latest
    steps:
      - name: Set Up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"
          cache: "gradle"